// js/Platform.js

import GameObject from './GameObject.js';

export default class Platform extends GameObject {
  // No additional constructor needed if we're only using the base properties
}
